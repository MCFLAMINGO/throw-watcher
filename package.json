{
  "name": "throw-watcher",
  "version": "1.0.0",
  "description": "THROW chain watcher — polls Tempo for Transfer events, sends Web Push notifications",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18"
  }
}
